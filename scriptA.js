// scriptA.js
// scriptA.js
window.executeA = function(LG, FV, N2, T) {
    console.log("Executing Script A with input:", LG, FV, N2, T);
    // 示例操作：将 FV 增加 10%
    FV = FV * 1.1;
    return [LG, FV, N2, T];
};
